import {Header,Footer,CTA} from '../components'; import {fleetServices} from '../fleet-content';
export const metadata={title:'Services',description:'Philippines web design teams services delivered by specialists based in the Philippines.'};
export default function Services(){return <><Header/><main className="fleet-main fleet-websitedesignoutsource"><section className="fleet-hero"><div className="container"><p className="eyebrow">Philippines-based services</p><h1>Philippines web design teams, scoped around real work</h1><p className="lead">Choose a focused service lane, then define the tools, examples, approvals, and reporting your Philippines-based specialist will use.</p></div></section><section className="section"><div className="container fleet-card-grid">{fleetServices.map((s,i)=><a className="fleet-card" href={`/services/${s.slug}`} key={s.slug}><span>{String(i+1).padStart(2,'0')}</span><h2>{s.title}</h2><p>{s.summary}</p><b>View service →</b></a>)}</div></section><CTA/></main><Footer/></>}
