import {notFound} from 'next/navigation'; import {Header,Footer,CTA,JsonLd} from '../../components'; import {fleetServices} from '../../fleet-content'; import {site} from '../../data';
export function generateStaticParams(){return fleetServices.map(s=>({slug:s.slug}))}
export async function generateMetadata({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const s=fleetServices.find(x=>x.slug===slug);if(!s)return {};return {title:s.title,description:s.summary}}
export default async function ServicePage({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const s=fleetServices.find(x=>x.slug===slug);if(!s)notFound();const url=`https://${site.domain.toLowerCase()}/services/${s.slug}`;return <><Header/><main className="fleet-main"><JsonLd data={{'@context':'https://schema.org','@type':'Service',name:s.title,description:s.summary,url,areaServed:'Philippines'}}/><section className="fleet-hero"><div className="container"><p className="eyebrow">Philippines service</p><h1>{s.title}</h1><p className="lead">{s.summary}</p><a className="btn primary" href="/contact">Discuss this service</a></div></section><section className="section"><div className="container fleet-detail-grid"><article><h2>Work this role can support</h2><ul>{s.tasks.map(x=><li key={x}>{x}</li>)}</ul></article><article><h2>Controls to set first</h2><ul>{s.controls.map(x=><li key={x}>{x}</li>)}</ul></article><article><h2>Practical launch plan</h2><ol>{s.launch.map(x=><li key={x}>{x}</li>)}</ol></article></div></section><CTA/></main><Footer/></>}
