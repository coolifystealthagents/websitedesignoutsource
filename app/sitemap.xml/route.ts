const aug19Paths=Object.entries(aug19Meta).map(([slug,value])=>`/${(value as {family:string}).family}/${slug}`);import aug19Meta from '../aug19-meta.json';import {fleetServices,researchPosts} from '../fleet-content';import {blogPosts} from '../data';import {contentBlogPosts,contentResearchPosts} from '../content-library';const base='https://websitedesignoutsource.com';export async function GET(){const pages=['','/services','/blog','/blog/top-25-web-design-outsourcing-companies','/research','/contact','/privacy','/terms','/cancellation-policy'];const allBlogPosts=[...contentBlogPosts,...blogPosts.filter(p=>!contentBlogPosts.some(c=>c.slug===p.slug))];const blogPages=Array.from({length:Math.max(1,Math.ceil(allBlogPosts.length/20))},(_,i)=>i+1).filter(n=>n>1).map(n=>`/blog/page/${n}`);const urls=[...aug19Paths,...pages,...fleetServices.map(s=>`/services/${s.slug}`),...allBlogPosts.map(p=>`/blog/${p.slug}`),...blogPages,...researchPosts.map(p=>`/research/${p.slug}`),...contentResearchPosts.map(p=>`/research/${p.slug}`)];const xml=`<?xml version="1.0" encoding="UTF-8"?><urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">${urls.map(x=>`<url><loc>${base}${x}</loc></url>`).join('')}</urlset>`;return new Response(xml,{headers:{'Content-Type':'application/xml'}})}
