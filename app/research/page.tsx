import {Header,Footer} from '../components';import {researchPosts} from '../fleet-content';import {contentResearchPosts} from '../content-library';export const metadata={title:'Research',description:'Sourced research for outsourced website design.'};export default function Research(){const posts=[...contentResearchPosts,...researchPosts.filter(p=>!contentResearchPosts.some(c=>c.slug===p.slug))];return <><Header/><main className="fleet-main"><section className="fleet-hero"><div className="container"><p className="eyebrow">Research library</p><h1>Research for outsourced website design</h1><p className="lead">Daily sourced articles covering QA, accessibility, SEO, and reliable website production.</p></div></section><section className="section"><div className="container fleet-card-grid">{posts.map(p=><a className="fleet-card" href={`/research/${p.slug}`} key={p.slug}><h2>{p.title}</h2><p>{p.excerpt}</p></a>)}</div></section></main><Footer/></>}
