import {Header,Footer} from '../components';import {researchPosts} from '../fleet-content';export const metadata={title:'Research',description:'Research notes about Philippines-based staffing and operations.'};export default function Research(){return <><Header/><main className="fleet-main"><section className="fleet-hero"><div className="container"><p className="eyebrow">Research library</p><h1>Research for planning Philippines-based teams</h1><p className="lead">This library will publish sourced articles about role design, operating controls, and staffing decisions. No research articles are available yet.</p></div></section><section className="section"><div className="container fleet-card-grid">{researchPosts.map(p=><a className="fleet-card" href={`/research/${p.slug}`} key={p.slug}><h2>{p.title}</h2><p>{p.excerpt}</p></a>)}</div></section></main><Footer/></>}
