import {Header,Footer} from '../components';import {publicTiers} from '../fleet-content';
export const metadata={title:'Pricing',description:'Public hourly pricing for Philippines-based assistant staffing.'};
export default function Pricing(){return <><Header/><main className="fleet-main fleet-websitedesignoutsource"><section className="fleet-hero"><div className="container"><p className="eyebrow">Simple public pricing</p><h1>Choose the support level that fits the work</h1><p className="lead">Each tier is for Philippines-based staffing. Role scope, schedule, tools, and approval rules are reviewed during intake.</p></div></section><section className="section"><div className="container pricing-grid">{publicTiers.map((t,i)=><article className={`price-card tier-${i+1}`} key={t.name}><p className="eyebrow">Tier {i+1}</p><h2>{t.name}</h2><p className="price">{t.price}</p><p>{t.detail}</p><ul><li>Role and task scoping</li><li>Philippines-based candidate matching</li><li>Onboarding and handoff planning</li></ul><a className="btn primary" href="/contact">Contact Us</a></article>)}</div></section><section className="section band"><div className="container"><h2>Not sure which tier fits?</h2><p>Send the positions, schedule, and work details. The intake gives the staffing team enough context to discuss the right Philippines-based support level.</p><a className="btn" href="/contact">Start the intake</a></div></section></main><Footer/></>}
