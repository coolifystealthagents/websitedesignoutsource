import {blogPosts} from '../../../data';import {BlogListing,PAGE_SIZE} from '../../blog-listing';export function generateStaticParams(){return Array.from({length:Math.max(1,Math.ceil(blogPosts.length/PAGE_SIZE))},(_,i)=>({page:String(i+1)}))}export default async function NumberedBlogPage({params}:{params:Promise<{page:string}>}){const {page}=await params;const n=Number(page);return <BlogListing page={n}/>} 
